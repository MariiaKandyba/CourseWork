<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
        <TextBlock Grid.Row="0" Text="Login" Margin="10" />
        <TextBox Grid.Row="0" Name="UsernameTextBox" Margin="10" />
            
        </StackPanel>
        <StackPanel Grid.Row="1">
            <TextBlock Grid.Row="1" Text="Password" Margin="10" />
            <PasswordBox Grid.Row="1" Name="PasswordBox" Margin="10" />

        </StackPanel>

        

        <Button Grid.Row="2" Content="Login" Name="LoginButton" Click="LoginButton_Click" Width="100" Height="30" Margin="10" />

        <TextBlock Grid.Row="3" Name="ErrorMessageText" Foreground="Red" Margin="10" />
    </Grid>
</Window>
