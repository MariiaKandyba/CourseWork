<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="Client" Height="270" Width="300" ResizeMode="NoResize" WindowState="Normal" FontFamily="Consolas" FontSize="14">

    <Window.Resources>

        <Style TargetType="Button" x:Key="ButtonStyle">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#FF4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF388E3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Grid.Row="0" Text="Login" Margin="10" />
            <TextBox Grid.Row="0" Text="mary_smith" Name="UsernameTextBox" Margin="10" />

        </StackPanel>
        <StackPanel Grid.Row="1">
            <TextBlock Grid.Row="1" Text="Password" Margin="10" />
            <PasswordBox Grid.Row="1" Password="pass123" Name="PasswordBox" Margin="10" />

        </StackPanel>
        

        <Button Grid.Row="2" Content="Login" Name="LoginButton" Style="{StaticResource ButtonStyle}" Click="LoginButton_Click" Width="100" Height="30" Margin="10" />
        <TextBlock Grid.Row="3" FontSize="10" Name="ErrorMessageText" Foreground="Red" Margin="10" />
    </Grid>
</Window>
