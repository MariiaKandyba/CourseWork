<Window x:Class="Client.Views.TestView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views"
        mc:Ignorable="d"
        Title="TestView" Height="450" Width="800">
   
    
    <!--<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" VerticalAlignment="Top" Margin="30 0 0 0">
            <Label Content="5" Name="QuestionLabel"/>
            <ItemsControl Name="AnswersListBox" Grid.Row="1" >
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <RadioButton Content="{Binding AnswerText}" GroupName="Answers" Checked="RadioButton_Checked" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
        
        <WrapPanel  Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Previous" Name="PrevBtn" Click="PrevBtn_Click" Width="100" Margin="0 0 5 0"  />
            <Button Content="Next" Name="NextBtn" Click="NextBtn_Click" Width="100" Margin="5  0 0 0"   />
        </WrapPanel>
        <Button Grid.Row="3" Content="Confirm Answers" Name="ConfirmBtn" Click="ConfirmBtn_Click" HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="100" Margin=" 0 0 0 50"  />

    </Grid>-->
    
    
</Window>
