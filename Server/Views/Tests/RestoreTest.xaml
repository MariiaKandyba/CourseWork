<Window x:Class="Server.Views.Tests.RestoreTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Server.Views.Tests"
        mc:Ignorable="d"
        Title="RestoreTest" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBar Background="White" >
            <Button Name="RestoreButton" Click="RestoreButton_Click">
                <StackPanel>
                    <Image Source="/Resources/restore.png" Width="25" Height="25" />
                    <Label Content="Restore User" FontSize="8"></Label>
                </StackPanel>
            </Button>
        </ToolBar>
            
                <ScrollViewer 
     VerticalScrollBarVisibility="Auto" 
     Grid.Row="1">
                    <DataGrid  ItemsSource="{Binding Tests}"  
                SelectedItem="{Binding SelectedTest}"
                Name="grid"
                VerticalAlignment="Top"
                 Style="{StaticResource ModernDataGridStyle}" 
                 ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeaderStyle}" 
                 RowStyle="{StaticResource ModernDataGridRowStyle}"
             SelectionMode="Extended"
                               
                 AutoGenerateColumns="False"
                 IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="Pass Percent" Binding="{Binding PassPercent, StringFormat={}{0}%}" Width="85" />
                            <DataGridTextColumn Header="Loaded Date" Binding="{Binding LoadedDate, StringFormat=d MMMM yyyy}" Width="*"/>
                        </DataGrid.Columns>

                    </DataGrid>
                </ScrollViewer>

    </Grid>
</Window>
